var expect = require('chai').expect;

var APPNEXUS = require('../../src');

describe('appnexus-html5-lib host', function () {
  it('creates a placement', function () {

      expect(true).to.equal(false);
  });
  it('ready is triggered when loaded', function () {
      expect(true).to.equal(false);
  });
});